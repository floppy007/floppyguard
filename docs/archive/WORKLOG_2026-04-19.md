# Worklog 2026-04-19

Stand: Ende des Arbeitstags

## Heute umgesetzt

- WireGuard-Domain zwischen Backend, Frontend und Doku weiter vereinheitlicht
- `/wireguard`, `/gateway` und `/platform` auf dieselben Link-, Reachability- und Planungsbegriffe gezogen
- Backend-Heuristiken fuer Link-Typen, Warnungen, Next Actions und `wg0 -> client` abgesichert
- Preview-/Review-Flow fuer Planner, Link-Metadaten und Interface-Metadaten eingebaut
- Dry-Run-Endpoint `POST /api/wireguard/plan-preview` umgesetzt
- echter `metadata-only`-Apply mit Backup umgesetzt:
  - `POST /api/wireguard/apply-metadata`
- Apply-Status mit Backups und Audit umgesetzt:
  - `GET /api/wireguard/apply-state`
- Restore fuer bekannte Metadaten-Backups umgesetzt:
  - `POST /api/wireguard/restore-metadata`
- WireGuard-UI zeigt jetzt:
  - Apply-Status
  - Backup-Liste
  - letzte Apply-/Restore-Aktion
  - Restore-Buttons fuer bekannte Backups
- Produktionspfad und SPA-Reload-Thema wurden frueher am Tag bereits stabilisiert

## Was jetzt real funktioniert

- Runtime-Read fuer WireGuard-Status
- Metadaten-Preview mit Diff, Warnungen und Apply-Entscheidung
- Metadaten-Apply fuer `metadata-only`
- Backup-Erzeugung vor Apply
- Backup-Liste und Apply-Audit
- Restore eines bekannten Metadaten-Backups

## Was bewusst noch nicht live ist

- kein Schreiben von `wg*.conf`
- kein `wg set`
- kein Peer-/Interface-CRUD auf dem Host
- kein Schreiben von Systemrouten
- kein Remote-Apply via SSH oder Agent

## Wichtig fuer morgen

Naechster sinnvoller Schritt:

- Restore nicht sofort hart ausfuehren, sondern zuerst einen Restore-Preview bauen
  - Backup lesen
  - Delta gegen aktuellen Metadatenstand zeigen
  - erst danach Restore bestaetigen

Danach:

- Audit-/Historienansicht ausbauen
- erst spaeter Richtung echtem Config-Write weitergehen
