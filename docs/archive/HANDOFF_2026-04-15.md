# FloppyGuard Handoff

Date: 2026-04-15

## Current State

The preview is working again and is usable:

- frontend preview: `http://your-instance-domain:5173/`
- backend preview health: `http://127.0.0.1:3300/`
- preview login:
  - email: `preview@example.com`
  - password: `Preview123!`

The app renders, login works, live NPM data was imported into preview, and the visible UI has already been heavily reworked toward FloppyGuard branding.

## Important Running Context

The preview backend was stabilized by running it in a PTY session instead of the older fragile background path.

Relevant env assumptions used for preview:

- `PORT=3300`
- `IP_RANGES_FETCH_ENABLED=false`
- `SKIP_CERTBOT_PLUGIN_SETUP=true`
- `INITIAL_ADMIN_EMAIL=preview@example.com`
- `INITIAL_ADMIN_PASSWORD=Preview123!`
- `DB_SQLITE_FILE=/var/www/nginx-proxy-manager-fork-base/.local-data/backend/database.sqlite`
- `NPM_LETSENCRYPT_ROOT=/opt/npm/letsencrypt`
- `NPM_CUSTOM_SSL_ROOT=/opt/npm/data/custom_ssl`

## What Was Fixed Earlier

### Rendering / runtime blockers

- fixed `react-dom/client` startup issue
- added Vite prebundle handling
- removed browser-path dependency problems from:
  - `humps`
  - `generate-password-browser`
- fixed backend token refresh bug in `backend/internal/token.js`

### Preview data / import

- imported live NPM data into preview sqlite
- preview sees imported proxy hosts, certificates, access lists, users, settings
- preview uses live certificate roots for display/read paths
- import helper exists:
  - `scripts/import-live-npm-to-preview`

### Branding / UI

- FloppyGuard logo assets were added and wired in
- login, setup, loading, header were rebranded
- top nav and layout were reworked
- major pages were visually updated:
  - dashboard
  - platform
  - gateway
  - wireguard
  - nginx host pages
  - admin list pages

### Locale / visible product text

- visible EN/DE/FR path was cleaned up
- language picker reduced to:
  - `en`
  - `de`
  - `fr`

### Version alignment

- frontend/backend version labels now show `2.14.0`
- preview backend health returns `2.14.0`

## 2.14.0 Baseline Check

This was checked against the live container and documented here:

- [LIVE_2_14_GAP.md](./LIVE_2_14_GAP.md)

Conclusion:

- live container really is `2.14.0`
- this repo is not a byte-identical mirror of live NPM
- it is best described as:
  - FloppyGuard Platform based on NPM `2.14.0`

## Backend Cleanup Already Done

### Reduced drift

- backend dependency versions were moved back closer to live `2.14.0`
- `backend/yarn.lock` was replaced with the live container lockfile
- duplicate route mounting in `backend/app.js` was removed

### Remaining intentional backend deltas

These are deliberate and should stay:

- `backend/routes/main.js`
  - `wireguard` route
  - health route supports both `""` and `"/"`
- `backend/index.js`
  - `PORT` from env
- `backend/setup.js`
  - `SKIP_CERTBOT_PLUGIN_SETUP`
- `backend/internal/certificate.js`
  - env-driven cert roots
- `backend/internal/token.js`
  - token bugfix
- `backend/internal/wireguard.js`
- `backend/routes/wireguard.js`

## Frontend Styling Work Already Done

### Global layout

- `frontend/src/App.css`
- `frontend/src/components/SiteHeader.module.css`
- `frontend/src/components/SiteMenu.module.css`
- `frontend/src/components/SiteContainer.module.css`

### Core page redesign

- login / setup
- dashboard / platform / gateway / wireguard
- nginx list pages
- admin list wrappers

### Modal / form styling

Global modal/form restyling was added to:

- `frontend/src/App.css`

Form sections upgraded:

- `src/components/Form/DNSProviderFields.tsx`
- `src/components/Form/DNSProviderFields.module.css`
- `src/components/Form/SSLOptionsFields.tsx`
- `src/components/Form/LocationsFields.tsx`
- `src/components/Form/LocationsFields.module.css`
- `src/components/Form/NginxConfigField.tsx`

## Important Unresolved UI Issue

There is still an unresolved styling problem in the large edit dialogs such as:

- `Proxy Host bearbeiten`

Observed status at end of day:

- modal transparency was improved substantially
- user reported:
  - first it was still too transparent
  - then it became better
  - latest remaining issue: upper part of the dialog was still clipped/cut off

Multiple CSS fixes were attempted in:

- `frontend/src/App.css`

including:

- making modal surfaces solid
- adding `platform-solid-modal`
- forcing fixed backgrounds with `!important`
- adjusting tab/header spacing
- changing modal/content overflow

Current risk:

- this area needs real DOM inspection in-browser, not more blind CSS guessing

## Recommended First Task Tomorrow

Do a real browser inspection of the live preview dialog before changing more CSS.

Target dialog:

- `Proxy Host bearbeiten`

Suggested approach:

1. open preview in browser
2. inspect the exact clipped element in devtools
3. identify whether the cut-off comes from:
   - `.modal-dialog`
   - `.modal-content`
   - `.modal-header`
   - `.card-header`
   - `.nav-tabs`
   - transform / margin / max-height / overflow chain
4. patch only that element chain

Do not continue broad global CSS edits until that exact DOM chain is confirmed.

## Demo File Saved

A standalone HTML mockup provided by the user was saved here:

- `/var/www/wg-gui/temp/floppyguard-proxy-hosts-demo.html`

Purpose:

- visual reference only
- not integrated into the NPM fork

## Files Most Relevant Tomorrow

### Backend

- `backend/app.js`
- `backend/index.js`
- `backend/routes/main.js`
- `backend/setup.js`
- `backend/internal/certificate.js`
- `backend/internal/token.js`

### Frontend

- `frontend/src/App.css`
- `frontend/src/modals/ProxyHostModal.tsx`
- `frontend/src/modals/RedirectionHostModal.tsx`
- `frontend/src/modals/DeadHostModal.tsx`
- `frontend/src/modals/StreamModal.tsx`
- `frontend/src/modals/AccessListModal.tsx`
- `frontend/src/components/Form/DNSProviderFields.tsx`
- `frontend/src/components/Form/SSLOptionsFields.tsx`
- `frontend/src/components/Form/LocationsFields.tsx`
- `frontend/src/components/Form/NginxConfigField.tsx`

## Verification Commands

Frontend smoke test:

- `cd /var/www/nginx-proxy-manager-fork-base/frontend && corepack yarn test --run src/App.smoke.test.tsx`

Backend health:

- `curl http://127.0.0.1:3300/`

WireGuard status:

- `curl http://127.0.0.1:3300/api/wireguard/status`

## Notes

- The recurring `DOMException [AbortError]` at the end of the frontend smoke test is from the happy-dom teardown path and did not indicate failing tests during this session.
- Cert display/read paths in preview currently point at live certificate trees. Viewing is okay; certificate write/destructive actions in preview should still be treated carefully.
